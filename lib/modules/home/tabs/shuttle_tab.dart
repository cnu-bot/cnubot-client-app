import 'package:flutter/material.dart';

class ShuttleTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Shuttle'),
    );
  }
}
